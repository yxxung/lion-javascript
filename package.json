{
  "scripts": {
    "start": "live-server client --host=localhost --port=5500 --no-browser"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "live-server": "^1.2.2",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    
  }
}
